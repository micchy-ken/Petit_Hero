{
  "collections": {
    "maps": {
      "description": "Game maps designed in the map editor",
      "fields": {
        "id": "string",
        "name": "string",
        "width": "number",
        "height": "number",
        "bgMode": "string",
        "bgImage": "string (optional)",
        "events": "array",
        "items": "array",
        "enemies": "array",
        "maxEnemies": "number"
      },
      "indexes": []
    },
    "enemy_assets": {
      "description": "Customized enemy assets and settings",
      "fields": {
        "id": "string",
        "enemyAssets": "map",
        "bossAssets": "map"
      },
      "indexes": []
    },
    "config": {
      "description": "Global custom configurations (assets, status, events, items, scenarios)",
      "fields": {
        "id": "string",
        "events": "array (optional)",
        "items": "array (optional)",
        "magics": "array (optional)",
        "scenarios": "array (optional)",
        "statusList": "array (optional)"
      },
      "indexes": []
    },
    "saves": {
      "description": "Game progress saves (saves/<scenarioId>, saves/shared_status, saves/metadata)",
      "fields": {
        "position": "map (optional)",
        "heroState": "map (optional)",
        "timestamp": "number (optional)",
        "lastPlayedScenarioId": "string (optional)"
      },
      "indexes": []
    }
  }
}
